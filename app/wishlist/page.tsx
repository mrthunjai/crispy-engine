import Header from '../components/Header'; import Footer from '../components/Footer'; import ProductCard from '../components/ProductCard'; import { products } from '../lib/data'; export default function Wishlist(){return <><Header/><main className="px-5 pb-24 pt-36 md:px-10"><p className="text-[10px] uppercase tracking-[.25em]">Saved for later</p><h1 className="mt-5 text-7xl tracking-[-.08em] md:text-[10rem]">Wishlist <span className="text-black/30">(3)</span></h1><div className="mt-16 grid gap-5 md:grid-cols-3">{products.slice(0,3).map(p=><ProductCard key={p.slug} p={p}/>)}</div></main><Footer/></>}
