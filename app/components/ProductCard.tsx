import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import { money } from '../lib/data'
export default function ProductCard({p}:{p:any}){return <article className="group"><Link href={`/product/${p.slug}`}><div className="relative aspect-[4/5] overflow-hidden bg-[#e5e1d8]"><img src={p.image} alt={p.name} className="h-full w-full object-cover transition duration-700 group-hover:scale-105"/>{p.tag&&<span className="absolute left-4 top-4 bg-white px-3 py-2 text-[9px] uppercase tracking-[.2em]">{p.tag}</span>}<span className="absolute bottom-4 right-4 flex h-10 w-10 items-center justify-center rounded-full bg-white opacity-0 transition group-hover:opacity-100"><ArrowUpRight size={16}/></span></div></Link><div className="flex justify-between pt-4 text-sm"><div><p>{p.name}</p><p className="mt-1 text-xs text-black/45">{p.type}</p></div><p>{money(p.price)}</p></div></article>}
