import Header from '../components/Header'; import Footer from '../components/Footer'; import ProductCard from '../components/ProductCard'; import { products } from '../lib/data'; export default function Search(){return <><Header/><main className="px-5 pb-24 pt-36 md:px-10"><p className="text-[10px] uppercase tracking-[.25em]">Find a piece</p><div className="mt-6 border-b border-ink pb-5 text-4xl tracking-tight">Search the collection</div><div className="mt-12 grid gap-5 md:grid-cols-3">{products.slice(0,3).map(p=><ProductCard key={p.slug} p={p}/>)}</div></main><Footer/></>}
